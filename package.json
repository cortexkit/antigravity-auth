{
  "name": "@cortexkit/antigravity-auth",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/opencode build && bun run --cwd packages/pi build",
    "typecheck": "bun run --cwd packages/core build && bun run --cwd packages/opencode typecheck && bun run --cwd packages/pi typecheck",
    "test": "bun run --cwd packages/core build && bun test --isolate",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "1.3",
    "@tsconfig/bun": "1.0.10",
    "@types/node": "24.10.1",
    "typescript": "6.0.3"
  }
}
